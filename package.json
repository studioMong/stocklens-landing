{
  "name": "stocklens-landing",
  "version": "1.0.0",
  "description": "StockLens landing page",
  "scripts": {
    "start": "serve . -p ${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
